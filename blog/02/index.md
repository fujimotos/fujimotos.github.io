# [Python] Python/CFFIを利用してC拡張を作成する (2/2)

本記事は全2回の連載の後編です。前回の記事は[こちらから](http://www.clear-code.com/blog/2018/1/17.html)読めます。

前回の記事では CFFIライブラリを使うことで、C関数のプロトタイプ宣言から
Pythonの拡張モジュールを自動的に生成できることを見ました。

この後編では、前編で処理できなかった（おそらく読者の皆さんにとって興味
深いと思われる）問題を順番に潰していきます。

## 自動生成だけでは対応できないケース

そもそもCFFIは移植対象の関数についてあらゆる情報を収集して、
可能な限り変換を行うという趣向のライブラリではありません。
たかだか読み込んでるのは入出力の型情報（プロトタイプ宣言）にすぎず、
そのため、引数や返り値の背後にあるセマンティクスについては全く関知していません。

これは、例えばCでlong型のポインタを受け取る関数があったとして、
それが関数に値を渡すための配列なのか、あるいは出力のための配列なのか、
あるいは配列ですらなく、ある単一の整数値を参照するポインタなのかは、
コードジェネレータ側からは区別できない、ということを意味しています。
したがって、「この引数は書き込み用のポインタだから、一時的なメモリ領域を取っておいて、
最後にPythonのリストに変換して返そう」という高次の判断を行う基礎はもとよりなく、
この部分の対応付けは専ら人間の手に委ねられることになります。

この構造的な制約が持つ含意は、実に深いと言わざるをません。
まず、本記事の執筆時点（2018年）において、CFFIと競合する他のライブラリも
軒並み同じ制約を抱えている、という事実を指摘したいと思います。

